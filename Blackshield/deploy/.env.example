# Kopiere diese Datei zu `.env` (im selben Ordner) und setze die Werte.
# `.env` ist gitignored — landet niemals im Repo.

POSTGRES_DB=blackshield
POSTGRES_USER=blackshield

# Lokal beliebig, Prod: openssl rand -base64 32
POSTGRES_PASSWORD=change-me-locally
